[build]
  command = "hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.128.0"
  HUGO_ENV = "production"

[[headers]]
  for = "/*.xml"
  [headers.values]
    Content-Type = "application/xml"